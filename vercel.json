{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://upgraied-backend.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/client/dist/index.html" }
  ]
}
