/**
 * Upgraied V2 shared components barrel.
 * Add V2-specific shared components here.
 *
 * Re-export common components from V1 that V2 can reuse:
 */
export { default as BloomGrid }     from '../../components/common/BloomGrid'
export { default as ErrorBoundary } from '../../components/common/ErrorBoundary'
