// TODO: Build VideoPlayer — see project spec
const VideoPlayer = () => (
  <div className="min-h-screen flex items-center justify-center">
    <h1 className="font-clash text-2xl grad-text">VideoPlayer</h1>
  </div>
);
export default VideoPlayer;
