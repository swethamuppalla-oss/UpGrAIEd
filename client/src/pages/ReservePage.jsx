// TODO: Build ReservePage — see project spec
const ReservePage = () => (
  <div className="min-h-screen flex items-center justify-center">
    <h1 className="font-clash text-2xl grad-text">ReservePage</h1>
  </div>
);
export default ReservePage;
